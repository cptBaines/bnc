# Set the default behavior, in case people don't have core.autocrlf set
* text=auto

# Explicitly declare text files you want to always be normalized and
# converted to native line endings on checkout
*.c text
*.h text

# Declare files that always have CRLF on checkout

# Declare files that always have LF on checkout
*.adoc eol=lf

# Denote all files that are truly binary and should not be modified
*.png binary
*.jpg binary
*.bmp binary
*.dat binary
*.fon binary
